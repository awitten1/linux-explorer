{
  "name": "linux-explorer",
  "type": "module",
  "dependencies": {
    "node-addon-api": "^8.1.0",
    "node-api-headers": "^1.3.0"
  },
  "scripts": {
    "build": "npm install && cmake -B build -DCMAKE_BUILD_TYPE=Release -G Ninja && cmake --build build",
    "clean": "rm -rf node_modules && rm -rf build"
  }
}
